import 'package:flutter/foundation.dart';
import 'package:flutter/widgets.dart';

class SkillRequstScreen extends StatelessWidget {
  const SkillRequstScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("SkillRequstScreen"),
    );
  }
}
